selenium==4.25.0
webdriver-manager==4.0.2
requests==2.32.3  # For any HTTP requests, if needed
pytest-html==4.1.1  # Optional: For generating HTML reports (not needed if using only unittest)
kubernetes~=31.0.0
